using UnityEngine;
using UnityEngine.UIElements;

namespace Ship
{
    public class ShipPart : MonoBehaviour
    {
        /*// Start is called before the first frame update
        void Start()
        {
            HealthSlider = root.Query<Slider>(name: "HealthSlider");
        }*/
        
        void HideSlider()
        {
            //HealthSlider.style.display = DisplayStyle.None;
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
